<table class="table table-bordered">
    <tr>
      <th>Date</th>
      <th>Recipient</th>
      <th>Amount</th>
      <th>Reason</th>
    </tr>
    <% @distributions_three.each do |distribution| %>
    <tr>
      <td><%= distribution.formatted_date %></td>
      <td><%= distribution.recipient.name %></td>
      <td><%= distribution.amount %></td>
      <td><%= distribution.description %></td>
    </tr>
  <% end %>
  </table>
