<% if @users_ids %>
  $("#modal-window").html("<%= escape_javascript(render 'allocate_money') %>");
  $("#modal-window").modal();
<% else %>
$('.ra-error').html('Please select a user or users');
<% end %>